<?xml version="1.0" encoding="utf-8"?>
<resources>
    <bool name="isTablet">false</bool>

    <!--
        2025-08-20 (audit-fix rev 7): `tellaDialogTheme` is now declared in
        the shared-ui module (so QuickDeletePinManager, which lives in
        shared-ui, can resolve it). The mobile `AppTheme.NoActionBar`
        theme overrides it to `@style/TellaDialogTheme` so every shared-ui
        dialog inherits the visible-button color scheme.
    -->

    <declare-styleable name="DonutProgress">
        <attr name="donut_progress" format="integer"/>
        <attr name="donut_max" format="integer"/>
        <attr name="donut_unfinished_color" format="color"/>
        <attr name="donut_finished_color" format="color"/>
        <attr name="donut_finished_stroke_width" format="dimension"/>
        <attr name="donut_unfinished_stroke_width" format="dimension"/>
        <attr name="donut_text_size" format="dimension"/>
        <attr name="donut_text_color" format="color"/>
        <attr name="donut_text" format="string"/>
        <attr name="donut_prefix_text" format="string"/>
        <attr name="donut_suffix_text" format="string"/>
        <attr name="donut_background_color" format="color"/>
    </declare-styleable>

    <declare-styleable name="InformationButton">
        <attr name="topText" format="reference|string" />
        <attr name="topTextCenter" format="reference|string" />
        <attr name="bottomTextCenter" format="reference|string" />
        <attr name="bottomText" format="reference|string" />
        <attr name="startImg" format="reference" />
        <attr name="state_checked" format="boolean"/>
    </declare-styleable>
</resources>
