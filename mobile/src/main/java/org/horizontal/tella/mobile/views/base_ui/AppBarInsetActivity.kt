package org.horizontal.tella.mobile.views.base_ui

/**
 * Marker for activities that host their own app bar and already handle the top system-bar inset.
 * Fragments shown inside these activities should not apply top window insets again.
 */
interface AppBarInsetActivity
